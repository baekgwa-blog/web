Never scan the entire repository unless explicitly instructed.

Strictly limit file reads to the task scope.

Avoid scanning the entire app directory unless explicitly required.

Only read specific route folders or directly related components.

Run TypeScript check only once per task.

Do not modify global styles unless explicitly requested.

Fix imports only in edited files.
